#!/bin/bash

case $1 in
  --dark | -d)
    hsetroot -fill ~/.config/theme/dark.jpg
    #hsetroot -solid '#232729' &
    ;;
  --light | -l)
    hsetroot -fill ~/.config/theme/light.jpg
    #hsetroot -solid '#FFFFFF' &
    ;;
  *)
    echo "Invalid use" 1>&2
    ;;
esac
